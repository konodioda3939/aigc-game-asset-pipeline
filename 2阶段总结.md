# 里程碑 2：Python 推理 API — 总结

> 目标：用 FastAPI 搭建本地 HTTP 服务，接收文字 prompt，返回 AI 生成的动漫风格图片。

---

## 最终文件结构

```
inference_server/
├── main.py              ← FastAPI 入口（/generate + /health）
├── model_loader.py      ← 加载 SD 模型 + LoRA 权重（全局单例）
├── start.bat            ← 小白双击启动脚本
├── requirements.txt     ← 依赖清单
└── outputs/             ← 生成图片自动存档（12 张）
```

---

## 2.1 model_loader.py — 模型加载模块 ✅

### 职责

服务启动时加载 Counterfeit-V2.5 + 原神风格 LoRA，全局只加载一次，之后所有请求复用。

### 流程

```
检测 GPU/CPU
    ↓
加载 Counterfeit-V2.5 基座模型（2GB，首次下载后缓存）
    ↓
通过 PEFT 挂载里程碑1训练的 LoRA（adapter_model.safetensors 12.2MB）
    ↓
merge_and_unload() 融合进 UNet → 变成标准 UNet（推理速度不变）
    ↓
配置 DPMSolver 调度器（25 步就能出好图）
    ↓
返回 pipeline 对象
```

### 关键设计

| 设计点 | 说明 |
|--------|------|
| 全局单例 | `_pipeline` 只加载一次，服务生命周期内复用 |
| PEFT 加载 | `PeftModel.from_pretrained()` → `merge_and_unload()`，不能直接用 `pipe.load_lora_weights()` |
| fp16 + GPU | CUDA 用 float16（8GB 显存够用），CPU 回退 float32 |
| attention_slicing | 把注意力计算切成小块，省显存 |
| 进度即时输出 | 所有 print 加 `flush=True`，启动时用户能看见每一步 |

---

## 2.2 main.py — FastAPI 服务 ✅

### 两个接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/generate` | POST | 传入 prompt，返回生成的图片 |
| `/health` | GET | 返回服务状态（就绪/加载中、GPU/CPU、已运行时长） |
| `/` | GET | 重定向到 `/docs` 可视化文档 |

### POST /generate 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `prompt` | 必填 | 无 | 正向提示词（英文），最长 1000 字符 |
| `negative_prompt` | 可选 | 自动屏蔽畸形/低画质/水印 | 负面提示词 |
| `steps` | 可选 | 25 | 推理步数（10~100），越多越精细但越慢 |
| `guidance_scale` | 可选 | 7.5 | 引导强度（1~20），越高越贴 prompt |
| `seed` | 可选 | 随机 | 固定随机种子，相同 seed+prompt=相同图 |

### 返回格式

- 图片以 PNG 文件流直接返回
- 响应头带 `X-Seed`（种子值）和 `X-Filename`（存档文件名）
- 每张图自动存档到 `outputs/` 目录

### 启动方式

```bash
# 方式1：双击（最简单）
start.bat

# 方式2：命令行
cd d:\aigc-project\inference_server
python -m uvicorn main:app --host 127.0.0.1 --port 8000

# 方式3：直接运行
python main.py
```

---

## 2.3 测试记录 ✅

### 命令行测试（3 个 prompt 并发）

| 测试 | prompt | HTTP | 大小 | 耗时 |
|------|--------|------|------|------|
| 原神角色 | `1girl, raiden shogun, purple hair, braid, japanese clothes, masterpiece` | 200 ✅ | 460 KB | 4.1s |
| 道具图标 | `a game sword icon, fantasy style, glowing, masterpiece` | 200 ✅ | 482 KB | 3.9s |
| 原神角色 | `1girl, furina, white hair, blue eyes, top hat, masterpiece` | 200 ✅ | 377 KB | 4.2s |

### 网页手动测试

通过 `http://127.0.0.1:8000/docs` 的 Try it out 功能测试通过 ✅

---

## 遇到的问题与解决

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| 1 | 用户输入 `uvicorn` 命令后黑屏无反应 | Windows 上 `uvicorn` 不在 PATH 环境变量中，命令未被执行 | 改用 `python -m uvicorn`，并创建 `start.bat` 双击启动 |
| 2 | 启动时看不到进度，用户以为卡了 | Python 的 `print()` 在缓冲模式下不会即时显示 | 所有 `print()` 加 `flush=True`，启动日志即时可见 |
| 3 | 测试时发现 `load_pipeline()` 在 FastAPI 启动事件中可能阻塞 | 同步函数在 async 事件循环中执行，但这本身是预期行为 | 实测加载只需 4 秒，不影响使用 |

---

## 核心经验

1. **`python -m uvicorn` 比 `uvicorn` 更可靠**：Windows 上 pip 安装的命令行工具不一定在 PATH 中，用 `python -m` 避免找不到命令
2. **print 要加 flush**：服务启动时的日志对用户很重要，不加 flush 会被缓冲，用户看不到进展会以为卡了
3. **给小白做 bat 启动脚本**：对技术小白来说，双击 `.bat` 文件比在命令行敲命令友好 100 倍
4. **FastAPI 自带 docs**：`/docs` 页面是给非技术人员最好的测试工具，填表单就能调接口

---

## 产出清单

```
inference_server/
├── main.py                ← FastAPI 服务（3 个接口）
├── model_loader.py        ← 模型加载（全局单例 + LoRA 融合）
├── start.bat              ← 双击启动
├── requirements.txt       ← 7 个依赖包
└── outputs/               ← 12 张测试生成图
```

---

> **里程碑2 状态：✅ 完成**。服务运行在 `http://127.0.0.1:8000`，本地可用 curl/浏览器/任何 HTTP 客户端调用。
