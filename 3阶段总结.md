# AIGC 项目 — 三阶段总结

## 项目目标

用 AI 自动生成游戏素材：输入文字描述 → AI 生成图片 → 在 Unity 引擎里直接使用。

---

## 阶段总览

```
┌──────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  阶段 1       │ ──→ │  阶段 2           │ ──→ │  阶段 3          │
│  LoRA 训练    │     │  Python 推理服务   │     │  Unity 编辑器插件 │
│              │     │                  │     │                 │
│  48张原神图   │     │  启动 API 服务     │     │  窗口输入文字     │
│  → 风格模型   │     │  发送 prompt      │     │  → 生成 → 导入   │
│              │     │  → 返回图片        │     │  → 拖到场景用    │
└──────────────┘     └──────────────────┘     └─────────────────┘
```

---

## 阶段 1：LoRA 风格微调 ✅

### 做了什么
- 收集了 48 张原神角色的 AI 绘图作为风格参考
- 用 Python 脚本对图片做了中心裁切（512×512）
- 使用 WD SwinV2 模型自动识别每张图的标签
- 以 Counterfeit-V2.5（动漫专用 SD 1.5）为基座模型
- 通过 LoRA 技术微调，让 SD 学会原神风格

### 产出
| 文件 | 说明 |
|------|------|
| `lora_output/adapter_model.safetensors` | LoRA 权重文件（12.2MB） |
| `train_lora.py` | 训练脚本 |
| `preprocess.py` | 图片预处理 |
| `caption.py` | 自动打标 |

### 关键技术
- LoRA rank=16，训练 1200 步
- 目标模块：Attention 投影层（to_q, to_k, to_v, to_out.0）
- 使用 GradScaler + clip_grad_norm 保证训练稳定

---

## 阶段 2：Python 推理 API ✅

### 做了什么
- 用 FastAPI 搭建了本地 HTTP 推理服务
- 把训练好的 LoRA 权重融合进基座模型（merge_and_unload）
- 提供 `/generate` 接口：发 prompt 过去，返回生成的图片
- 提供 `/health` 接口：检查服务是否在线

### 产出
| 文件 | 说明 |
|------|------|
| `inference_server/main.py` | FastAPI 入口（/generate + /health） |
| `inference_server/model_loader.py` | 模型加载器（SD + LoRA 融合） |
| `inference_server/start.bat` | 双击启动脚本 |
| `inference_server/requirements.txt` | 依赖清单 |

### 启动方式
```
双击 inference_server\start.bat
```
启动后浏览器访问 `http://127.0.0.1:8000/docs` 可以可视化测试。

### 接口参数
| 参数 | 默认值 | 说明 |
|------|--------|------|
| prompt | 必填 | 英文画面描述 |
| negative_prompt | 预设 | 不想出现的元素 |
| steps | 25 | 推理步数（10~100） |
| guidance_scale | 7.5 | 引导强度（1~20） |
| seed | 随机 | 固定种子可复现相同结果 |

---

## 阶段 3：Unity Editor 插件 ✅

### 做了什么
- 在 Unity 编辑器里做了一个可视化窗口
- 窗口里输入文字 → 调 Python 服务生成图片 → 预览 → 导入为资产
- 无需离开 Unity，整个流程在一个窗口里完成

### 产出
| 文件 | 说明 |
|------|------|
| `unity_plugin/Assets/Editor/AIGCAssetGenerator/AIGCWindow.cs` | Editor 窗口 UI |
| `unity_plugin/Assets/Editor/AIGCAssetGenerator/AIGCClient.cs` | HTTP 通信（调用推理服务） |
| `unity_plugin/Assets/Editor/AIGCAssetGenerator/AssetImporter.cs` | 图片保存为 Unity 资产 |
| `unity_plugin/Assets/Editor/AIGCAssetGenerator/AIGCSettings.cs` | 全局配置（Preferences 面板） |
| `unity_plugin/README.md` | 安装和使用说明 |

### 窗口功能
```
┌─────────────────────────────────────┐
│  AI 资产生成器                       │
│                                     │
│  ● 服务在线               [重新检测] │
│                                     │
│  画面描述: [_____________________]   │
│  资产类型:  [图标] [贴图] [UI元素]   │
│  高级选项: ▶ 折叠                    │
│                                     │
│  [      生成      ]                 │
│                                     │
│  预览区: (生成的图片)                 │
│                                     │
│  [导入到项目]  [打开输出文件夹]        │
└─────────────────────────────────────┘
```

### 使用流程
1. 双击 `start.bat` 启动 Python 推理服务
2. Unity 菜单栏 → Tools → AI Asset Generator
3. 输入英文描述，选资产类型，点「生成」
4. 预览满意后点「导入到项目」
5. 图片自动出现在 `Assets/GeneratedAssets/`，可拖到场景使用

---

## 最终项目结构

```
aigc-project/
├── data/
│   ├── style_images/            ← 48张原始参考图
│   └── processed/               ← 裁切后的 512×512 png + 标注
├── lora_output/
│   └── adapter_model.safetensors  ← LoRA 权重（12.2MB）
├── inference_server/
│   ├── main.py                  ← FastAPI 推理服务
│   ├── model_loader.py          ← SD + LoRA 加载器
│   ├── start.bat                ← 双击启动
│   └── outputs/                 ← 生成图片存档
├── unity_plugin/
│   └── Assets/Editor/AIGCAssetGenerator/
│       ├── AIGCWindow.cs        ← Unity 窗口
│       ├── AIGCClient.cs        ← HTTP 客户端
│       ├── AssetImporter.cs     ← 资产导入
│       └── AIGCSettings.cs      ← 全局配置
├── 3阶段总结.md                  ← 本文件
├── CLAUDE.md                    ← 项目说明（AI 协作指南）
├── train_lora.py                ← LoRA 训练脚本
├── preprocess.py                ← 图片预处理
└── caption.py                   ← 自动打标
```

---

## 技术栈一览

| 层级 | 技术 |
|------|------|
| 深度学习框架 | PyTorch + CUDA |
| 模型生态 | HuggingFace Diffusers + PEFT |
| 基座模型 | Counterfeit-V2.5（动漫 SD 1.5） |
| 打标模型 | WD SwinV2 Tagger v3（ONNX） |
| 推理服务 | FastAPI + Uvicorn |
| 游戏引擎 | Unity 2022.3 LTS |
| GPU | NVIDIA RTX 4060 Laptop（8GB） |

---

## 下一步建议

1. **批量生成**：一次生成多种变体（不同种子），从中挑选最好的
2. **参数预设**：针对不同游戏类型（RPG、科幻、卡通）保存不同的参数配置
3. **图片后期**：在 Unity 里加上简单的亮度/对比度/裁剪调整
4. **风格扩展**：用其他风格图集训练新的 LoRA，打造更多风格选择
5. **录制演示视频**：2 分钟录屏展示完整流程，面试最有说服力的素材
