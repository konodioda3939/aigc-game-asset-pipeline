# 里程碑 1：LoRA 风格微调 — 总结

> 目标：用 30-50 张参考图训练 LoRA，让 Stable Diffusion 学会画原神动漫风格。

---

## 整体架构

```
Pinterest/Danbooru 收集
       ↓
48张原神风格图 (data/style_images/)
       ↓ 1.2 preprocess.py
512×512 中心裁切 (data/processed/)
       ↓ 1.3 caption.py (WD14 ONNX)
自动打标 → 人工修正 (data/processed/*.txt)
       ↓ 1.4 train_lora.py
Counterfeit-V2.5 + LoRA (rank=16, 1200步)
       ↓
lora_output/adapter_model.safetensors (12.2MB)
```

---

## 1.1 选风格 + 收集数据 ✅

- **风格方向**：原神（Genshin Impact）日式动漫渲染风格
- **数据量**：48张 `.jpg`，来自 Pinterest/Danbooru/ArtStation
- **覆盖角色**：雷电将军、芙宁娜、胡桃、纳西妲、枫原万叶、荧、妮露 等
- **数据质量**：47张分辨率达标，1张偏低（509×720）

---

## 1.2 裁剪预处理 ✅

- **脚本**：`preprocess.py`
- **逻辑**：中心裁切为正方形 → `LANCZOS` 插值到 512×512 → 输出 `.png`
- **输出**：`data/processed/` 下 48 张图，全部成功

---

## 1.3 自动打标（问题最多，共解决 6 个关键问题）

### 最终方案

- **模型**：`SmilingWolf/wd-swinv2-tagger-v3`（SwinV2-Base，10861 个 Danbooru 标签）
- **推理方式**：ONNX Runtime（CPU）
- **输入**：NHWC 格式，原始 `[0, 255]` 像素值（不归一化）
- **阈值**：置信度 0.35

### 问题与解决

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| 1 | `WinError 10060` 连接 HuggingFace 超时 | 国内网络墙 HF | 设 `os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'`，**必须在 `import transformers` 之前** |
| 2 | `ValueError: Unrecognized model` | `wd-v1-4-vit-tagger-v2` 的 config.json 缺少 `model_type`，`AutoModel` 无法推断架构 | 改用 `ViTForImageClassification` 直接指定架构 |
| 3 | `OSError: no pytorch_model.bin` | 该模型只发布了 ONNX 权重，无 PyTorch 格式 | 切到 ONNX Runtime（`onnxruntime`） |
| 4 | 所有图标签只有 `monochrome, greyscale`（v1 模型） | 归一化方式不对（试了 `[0,1]`、`[-1,1]`、ImageNet、CLIP 四种都失败） | 换 v3 模型：`SmilingWolf/wd-swinv2-tagger-v3`，有完整 config.json |
| 5 | v3 模型同样全标 `monochrome, greyscale`，**连纯红色图都被标成灰度** | ONNX 模型**内嵌了预处理**（`Transpose(NHWC→NCHW) → /127.5 → -1.0`），外面又归一化一遍导致颜色被抹平 | `onnx.load()` 检查模型图前 3 个节点 → 发现内嵌预处理 → **只传原始 `[0,255]` 像素值，不做任何外部归一化** |
| 6 | 输入 shape 不匹配 | WD14 ONNX 期望 NHWC（`[1, 448, 448, 3]`） | 从 `session.get_inputs()[0].shape` 自动检测格式 |

### 关键发现

> **ONNX 模型调试最佳实践**：用 `onnx.load()` + 检查 `graph.node[:5]` 可以看到内嵌的预处理节点，避免盲目猜测归一化方式。

### 人工修正标注

用千问 VLM 交叉验证，发现 WD14 的典型盲区：

| 错误类型 | 示例 | 修正 |
|----------|------|------|
| 颜色误判 | `yellow_eyes` → 实际是蓝色 | `blue_eyes` |
| 颜色误判 | `blonde_hair` → 实际是橙红色 | `red_hair` |
| 语义误解 | `fake_horns` → 角色本身有角 | 删除 |
| 服装误分类 | `skirt` → 实际是裙裤/分体装 | 删除 |
| 漏掉构图标签 | 无 | 补充 `dynamic_angle`, `splashing_water`, `dramatic_lighting` |

---

## 1.4 LoRA 训练（2次训练，解决 3 个关键问题）

### 最终训练配置

| 参数 | 值 |
|------|-----|
| 基座模型 | `gsdf/Counterfeit-V2.5`（动漫专用 SD 1.5 微调） |
| 分辨率 | 512×512 |
| LoRA rank | 16 |
| 学习率 | 1e-4 |
| 等效 batch | 4（1 × 梯度累积 4） |
| 训练步数 | 1200（~100 epoch） |
| 可训参数 | 3.19M / 862.71M（0.37%） |
| 权重文件大小 | 12.2 MB |
| 目标模块 | `to_q`, `to_k`, `to_v`, `to_out.0`（仅 Attention 投影层） |

### 训练流程（3 阶段）

```
Phase 1: VAE 编码 → 48张图 → 潜空间向量 [1,4,64,64] → 缓存
Phase 2: CLIP 编码 → 48条caption → 文本嵌入 [1,77,768] → 缓存
Phase 3: UNet LoRA → 1200步，随机采样缓存数据训练
```

### 问题与解决

| # | 问题 | 原因 | 解决 |
|---|------|------|------|
| 1 | `ValueError: Attempting to unscale FP16 gradients` | `unet.to(device, dtype=torch.float16)` 把 PEFT 新加的 LoRA 层也转成了 fp16，GradScaler 拒绝处理 fp16 梯度 | `unet.to(device)` 不传 dtype，保留 LoRA 为 fp32 |
| 2 | 训练完成但所有权重为 NaN | 去掉 GradScaler 后 fp16 前向运算溢出，梯度爆炸 | 加回 `torch.amp.GradScaler('cuda')` + `clip_grad_norm_(max_norm=1.0)` |
| 3 | 推理时有无 LoRA 几乎无区别 | LoRA 用 SD 1.5 训练，推理用了 Counterfeit-V2.5，**权重空间不匹配** | 在 Counterfeit-V2.5 上重新训练 |

### 核心教训

1. **PEFT + fp16**：`model.to(device)` 不要带 `dtype` 参数，否则 LoRA 层会被意外转为 fp16
2. **GradScaler + clip_grad_norm**：fp16 混合精度训练两个都要有，一个防 underflow，一个防 overflow
3. **LoRA 基座必须一致**：在哪个模型上训练的，就必须在同一个模型上推理，跨模型不通用

---

## 1.5 验证效果 ✅

### 问题与解决

| 问题 | 原因 | 解决 |
|------|------|------|
| `pipe.load_lora_weights()` 加载后无任何效果 | diffusers 的 `load_lora_weights()` 不兼容 PEFT 保存的 key 命名格式（`base_model.model.xxx.lora_A.weight` vs diffusers 期望的格式） | 用 `PeftModel.from_pretrained(pipe.unet, path)` → `merge_and_unload()` 融进 UNet |
| `anything-v4.0` 无法下载 | hf-mirror 未收录/需要认证 | 换 `gsdf/Counterfeit-V2.5` |
| 生成图质量差 | 原版 SD 1.5 对动漫风格生成能力有限 | 用动漫专用基座模型 `Counterfeit-V2.5` |

### 推理脚本

`inference_compare.py`：同一组 prompt + 同 seed → `without_lora_*.png` / `with_lora_*.png` 对比

---

## 最终产出

```
data/
├── style_images/                 ← 原始图片（48张 jpg）
└── processed/                    ← 512裁切 png + 人工修正后的 .txt 标注

lora_output/
├── adapter_model.safetensors     ← 🔑 核心产出（12.2MB）
├── adapter_config.json           ← LoRA 配置
├── checkpoint-200~1200/          ← 训练中间保存点
└── comparison/                   ← 1.5 加载前后对比图
    ├── without_lora_01~08.png
    └── with_lora_01~08.png
```

---

## 涉及文件

| 文件 | 用途 |
|------|------|
| `preprocess.py` | 1.2 图片裁剪预处理 |
| `caption.py` | 1.3 WD14 ONNX 自动打标 |
| `check_env.py` | 环境检测（GPU、PyTorch、CUDA） |
| `check_images.py` | 图片质量检查 |
| `review_tags.py` | 标注审查工具（打印所有标签） |
| `fix_tags.py` | 批量修正标注（全局增删标签） |
| `train_lora.py` | 1.4 LoRA 训练脚本 |
| `inference_compare.py` | 1.5 推理对比脚本 |
